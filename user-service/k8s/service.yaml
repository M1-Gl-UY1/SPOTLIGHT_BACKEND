apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: microservices
spec:
  selector:
    app: user-service
  ports:
      port: 5000        # Port exposé dans le cluster
      targetPort: 5000  # Port dans le container
  type: ClusterIP
